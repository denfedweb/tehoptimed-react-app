import React, {Fragment} from 'react';
import Logo from "./Logo/Logo";

const Header = () => {
    return (
        <Fragment>
            <Logo />
        </Fragment>
    );
};

export default Header;
